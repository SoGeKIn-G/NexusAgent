<?xml version="1.0" encoding="utf-8"?>
<!--
    Capability declaration for the agent's perception + execution layer.

    canRetrieveWindowContent  reads the view hierarchy (perception)
    canPerformGestures        dispatchGesture() for taps and swipes (execution, M3)
    canTakeScreenshot         API 30+ frame capture with no MediaProjection consent
                              dialog and no foreground-service ceremony (M2)
    flagRequestFilterKeyEvents  needed for the volume-key kill switch (M6)

    notificationTimeout is deliberately low: we want events promptly, and the
    settle-detector in NexusAccessibilityService does the throttling instead —
    debouncing in the service is far cheaper than asking the system to batch.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds|flagRetrieveInteractiveWindows|flagIncludeNotImportantViews|flagRequestFilterKeyEvents"
    android:canPerformGestures="true"
    android:canRequestFilterKeyEvents="true"
    android:canRetrieveWindowContent="true"
    android:canTakeScreenshot="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="50"
    android:summary="@string/accessibility_service_summary" />
